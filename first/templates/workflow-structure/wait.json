{
  "id": "activity-id",
  "displayName": "...",
  "type": "wait",
  "delay": { "type": "expression type", "value": "Optional expression. Expression value is interpreted as delay in milliseconds." },
  "until": { "type": "expression type", "value": "Optional expression. Expression value is interpreted as date/time to wait until." },
  "body": {
    "comment": "Activity to be executed after the wait period is over."
  }
}
